<!DOCTYPE html>
<html lang="id">
<head>
    <meta charset="UTF-8">
    <meta name="viewport" content="width=device-width, initial-scale=1.0">
    <title>Daftar Pembayaran</title>
    <link rel="stylesheet" href="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/css/bootstrap.min.css">
</head>
<body>
    <%- include('../partials/navbar') %>

    <div class="container mt-4">
        <h2 class="mb-3">Daftar Pembayaran</h2>
        <a href="/pembayaran/create" class="btn btn-success mb-3">Tambah Pembayaran</a>

        <table class="table table-bordered table-striped">
            <thead class="table-dark">
                <tr>
                    <th>ID Pembayaran</th>
                    <th>No Antrian</th>
                    <th>Tipe Pembayaran</th>
                    <th>Nama Instansi</th>
                    <th>Aksi</th>
                </tr>
            </thead>
            <tbody>
                <% pembayaran.forEach((p) => { %>
                    <tr>
                        <td><%= p.id_pembayaran %></td>
                        <td><%= p.no_antrian %></td>
                        <td><%= p.tipe_pembayaran %></td>
                        <td><%= p.nama_instansi %></td>
                        <td>
                            <a href="/pembayaran/edit/<%= p.id_pembayaran %>" class="btn btn-warning btn-sm">Edit</a>
                            <a href="/pembayaran/delete/<%= p.id_pembayaran %>" class="btn btn-danger btn-sm" onclick="return confirm('Yakin ingin menghapus?')">Hapus</a>
                        </td>
                    </tr>
                <% }) %>
            </tbody>
        </table>
    </div>

    <script src="https://cdn.jsdelivr.net/npm/bootstrap@5.3.0/dist/js/bootstrap.bundle.min.js"></script>
</body>
</html>
